# Aggiungere controlli sicurezza su tutti gli uploader
# Caricando un file con lo stesso nome di uno già caricato, rimettere lo stesso nome dopo l'errore implementare nuovamente controllo
# RIVEDERE HOMEFEED - da spostare fuori e/o da variabilizzare se arriviamo da categories o fanta

// header("Content-Type: application/octet-stream");
    // header("Content-Transfer-Encoding: Binary");
    // header("Content-disposition: attachment; filename=\"".$files[2]."\"");
    // echo readfile($url);
    // rsort($files);
    //
    // print_r($files);